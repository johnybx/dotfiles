hl.config({
	plugin = {
		hyprcapture = {
			default_mode = "region",
			fusion_mode = true,
			confirm_before_capture = false,
			fullscreen_scope = "per-monitor",
			window_background = "follow-system",
			save = true,
			clipboard = true,
			show_thumbnail = true,
			save_dir = "/tmp/screenshot",
			record_save_dir = "/tmp/screen_recording",
		},
	},
})
